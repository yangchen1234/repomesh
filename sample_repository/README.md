# ParcelFlow sample repository

ParcelFlow is a deliberately small, polyglot codebase used by RepoMesh's reproducible retrieval evaluation. It models order checkout, inventory reservation, authentication, event routing, retries, job scheduling, payments, graph traversal, and a status page.

